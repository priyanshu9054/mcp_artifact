# Welcome to your prefect.yaml file! You can use this file for
# storing and managing configuration for deploying your flows.
# We recommend committing this file to source control along
# with your flow code.

# Generic metadata about this project
name: mcp_artifact
prefect-version: 3.7.5

# build section allows you to manage and build docker images
build: null

# push section allows you to manage if and how this project
# is uploaded to remote locations
push: null

# pull section allows you to provide instructions for cloning
# this project in remote locations
pull:
- prefect.deployments.steps.run_shell_script:
    id: get-github-token
    script: uv tool run prefect-cloud github token priyanshu9054/mcp_artifact
- prefect.deployments.steps.git_clone:
    id: git-clone
    repository: https://x-access-token:{{ get-github-token.stdout }}@github.com/priyanshu9054/mcp_artifact.git
    branch: mark_1

# the deployments section allows you to provide configuration
# for deploying flows
deployments:
- name: mcp-artifact
  version: null
  tags: []
  description: null
  schedule: {}
  flow_name: run_mcp
  entrypoint: main.py:run_mcp
  parameters: {}
  work_pool:
    name: default-work-pool
    work_queue_name: default
    job_variables:
      pip_packages:
        - fastmcp>=3.3.1
        - python-dotenv
        - prefect-cloud>=0.1.11
